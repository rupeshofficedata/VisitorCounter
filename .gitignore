# Python build artifacts
__pycache__/
*.pyc

# Binaries
kubectl
vcluster
get_helm.sh
kubectl.sha256

# Saved dumps
curl_response

# Helm chart values (not app code)
prometheus.yml

# opencode files
AGENTS.md
opencode.json
skills-lock.json
.agents/
.claude/skills/

# Personal interview prep / learning materials (not project code)
DevopsMD/
DevopsMD.zip
DevOps-Mastery-Vault/

# Terraform local state and provider plugin cache - never committed.
# .terraform.lock.hcl IS committed (pins exact provider versions for everyone).
terraform/**/.terraform/
terraform/**/terraform.tfstate
terraform/**/terraform.tfstate.backup
terraform/**/*.tfplan
